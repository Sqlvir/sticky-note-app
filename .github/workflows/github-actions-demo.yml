name: Hello World
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - run: ddddldlldld "Build step..."

  Test:
    needs: Build
    runs-on: ubuntu-latest
    steps:
      - run: echo "Test step..."
